//(counter_sliceStart until counter_sliceStart + counter_slice_size-1).map(x => x % t)

val counter_sliceStart = 7
val counter_slice_size = 6
val t = 10
val x = (counter_sliceStart until counter_sliceStart + counter_slice_size-1).map(x => x % t)
